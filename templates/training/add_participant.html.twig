{% extends 'base.html.twig' %}

{% block title %}Ajouter formateur{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary envoyer">{{ button_label|default('Ajouter') }}</button>
    {{ form_end(form) }}

{% endblock %}