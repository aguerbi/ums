{{ form_start(form) }}
<div class="row">
    <div class="col-6">
        {{ form_row(form.name) }}
    </div>
</div>
<button class="btn btn-primary envoyer">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
